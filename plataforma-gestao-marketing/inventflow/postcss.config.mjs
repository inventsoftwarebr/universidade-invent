const config = {
  // Sem framework de CSS: o arquivo existe para impedir que a configuração
  // PostCSS do projeto vizinho (Universidade Invent) seja herdada.
  plugins: {},
};

export default config;
